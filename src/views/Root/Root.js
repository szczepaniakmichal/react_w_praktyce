import React from 'react';
import Button from 'components/Button/Button';

const Root = () => (
  <div className="App">
    <Button width="300px">save / close</Button>
    <Button secondBtn>remove</Button>
  </div>
);

export default Root;
